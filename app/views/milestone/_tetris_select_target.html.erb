<h1>일감을 자동배치할 대상을 선택해 주세요 </h1>
<span style="color: #999;">추정 작업 시간이 기재되어 있어야 일감 자동 배치가 가능합니다.</span>
<br><br>

<div class="tabs">
  <ul>
    <li><a href="#user-tab" class="tab-link selected">사용자 단위 자동 배치</a></li>
    <li><a href="#parent-tab" class="tab-link">메인 일감 단위 자동 배치</a></li>
  </ul>
</div>

<div class="tab-content">
  <div id="user-tab" class="tab-pane active">
    <%= render :partial => 'milestone/tetris_select_target_user' %>
  </div>
  <div id="parent-tab" class="tab-pane">
    <%= render :partial => 'milestone/tetris_select_target_issue' %>
  </div>  
</div>

<script>
$(document).ready(function() {
  // 탭 클릭 이벤트
  $('.tab-link').click(function(e) {
    e.preventDefault();
    
    // 모든 탭에서 selected 클래스 제거
    $('.tab-link').removeClass('selected');
    
    // 모든 탭 컨텐츠에서 active 클래스 제거
    $('.tab-pane').removeClass('active');
    
    // 클릭된 탭에 selected 클래스 추가
    $(this).addClass('selected');
    
    // 해당 탭 컨텐츠에 active 클래스 추가
    var targetId = $(this).attr('href');
    $(targetId).addClass('active');
  });
});
</script>

<style>

.tab-pane {
  display: none;
}

.tab-pane.active {
  display: block;
}
</style>